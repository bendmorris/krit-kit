struct ImageData {
    public var texture: GLuint;
    public var dimensions: Rectangle[Int];

    rules {
        ($this.width) => $this.dimensions.width;
        ($this.height) => $this.dimensions.height;
    }
}
